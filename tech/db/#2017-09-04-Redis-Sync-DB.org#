* Redis与数据库同步解决方案

首先，我们要看是将redis中的数据同步到数据库中去，
还是将数据库中的数据同步到redis中去。

这和redis的使用方式有关：到底是做缓存还是做db？
如果是做缓存，按照缓存的语义来：
读：缓存有，读缓存；没有，将db中的数据载入到缓存中，读缓存；
写：写db，将缓存中的失效掉；

如果是redis做db，那么真的db（数据库）就只是一个备份了。
首先要考虑的就是会丢失数据吗，还好我们可以开启redis的持久化机制。
但是有个问题就是，持久量大了，redis的效率巨降的问题。
还有就是如果redis集群挂了，长时间的启动和数据加热问题。


